// Example 2: Sigmoid Function
// Tests neural activation function

fn main() {
    learn x = 2.0;
    let z = x * 3.0 + 1.0;
    let y = sigmoid(z);
    return y;
}

// sigmoid(7) = 1 / (1 + e^-7) = 0.999089
